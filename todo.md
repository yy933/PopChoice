# 📋 MVP Development To-Do List

## Phase 1: Setup & Infrastructure
- [x] **Create Next.js Project**
  - Use App Router + TypeScript + Tailwind CSS (`npx create-next-app@latest`).
- [x] **Supabase Setup & Enable Vector Extension**
  - Create a new project in the Supabase dashboard.
  - Run `CREATE EXTENSION IF NOT EXISTS vector;` in the SQL Editor to enable vector support.
- [x] **Create Database Table & RPC Functions in Supabase**
  - Create a `movies` table (including `id`, `title`, `release_year`, `content`, and `embedding` fields).
  - Configure vector index and write an RPC function for similarity search (e.g., `match_movies`).
- [ ] **Environment Variables Configuration (`.env.local`)**
  - Set `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
  - Set `OPENROUTER_API_KEY`.

---

## Phase 2: Data Ingestion & Embedding
> **Core Concept:** Convert the movie data from `content.js` into embeddings and store them in Supabase.

- [ ] **Choose an Embedding Model**
  - Recommended: Use a free embedding model via OpenRouter or HuggingFace (e.g., `text-embedding-3-small` or an open-source embedding API / HuggingFace Inference API).
- [ ] **Write Data Ingestion Script**
  - Read the movie array from `content.js`.
  - Iterate through each movie and combine `title` + `release_year` + `content` into a single text block.
  - Call the Embedding API to obtain the vector array for each text block.
  - Insert the movie metadata and vector data into the Supabase `movies` table.

---

## Phase 3: Backend API Routes
> **Core Workflow:** Receive quiz input -> Generate embedding -> Query Supabase for top match -> Generate LLM recommendation reason.

- [ ] **Create `/api/recommend` API Route (Next.js Route Handler)**
  1. **Receive Request:** Accept the combined user preference string from the frontend.
  2. **Generate Query Embedding:** Send this string to the Embedding API to get `user_embedding`.
  3. **Vector Search:** Call the Supabase RPC (`match_movies`) to find the Top 1 movie with the highest cosine similarity.
  4. **LLM Reason Generation:** Use a free OpenRouter model (e.g., `meta-llama/llama-3-8b-instruct:free` or `google/gemma-2-9b-it:free`) with a system prompt:
     > *"Based on the user's mood/preferences [User Input] and this movie [Movie Data], explain in an engaging tone why this movie is a perfect fit for them."*
  5. **Return Response:** Send the recommended movie details and the generated reason back to the client.

---

## Phase 4: Frontend UI & Interaction
> **Goal:** Guide users through the questionnaire step-by-step and present the recommendation.

- [ ] **Questionnaire Form Component**
  - Build the quiz form containing open-ended and multiple-choice questions.
  - Bind state to combine all responses into a single string describing mood and preferences.
- [ ] **Loading State Component**
  - Display a loading screen/animation upon clicking the submit button (e.g., *"Searching the movie database for you..."*).
- [ ] **Result View Component**
  - Display the matched movie title, release year, and detailed information.
  - Render the personalized recommendation text generated by the OpenRouter model.

---

## Phase 5: Verification & Testing
- [ ] **Preference Testing:** Test diverse prompt inputs (e.g., comedy vs. suspense/thriller) to verify whether Supabase vector queries return relevant results.
- [ ] **API Stability Check:** Verify OpenRouter free model rate limits and response consistency.

---

# 🚀 Post-MVP Stretch Goals

Once the MVP core requirements are completed, you can move on to these enhancements:

- [ ] **Multi-user Support ($n$ people):** Calculate a combined preference vector across multiple attendees.
- [ ] **Poster API Integration:** Connect to OMDb or TMDB API to fetch movie posters.
- [ ] **"Next Movie" Feature:** Retrieve the Top 3–5 similarity matches from Supabase RPC, allowing users to toggle through alternate recommendations.